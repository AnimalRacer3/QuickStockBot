# ─── Web env vars ─────────────────────────────────────────────────────────────
# Copy to .env.local and fill in real values. Never commit .env.local to git.

# URL of the Relay HTTP API (used server-side and client-side)
NEXT_PUBLIC_RELAY_API_URL=http://localhost:3001

# URL of the Relay WebSocket endpoint (client-side only)
NEXT_PUBLIC_RELAY_WS_URL=ws://localhost:8080

# NextAuth / session secret (generate with: openssl rand -hex 32)
NEXTAUTH_SECRET=

# App base URL (used by NextAuth for callbacks)
NEXTAUTH_URL=http://localhost:3000

# ─── Section 12: Licensing ────────────────────────────────────────────────────

# Resend API key for transactional email (https://resend.com)
RESEND_API_KEY=

# From address for license delivery emails
EMAIL_FROM=QuickStockBot <noreply@quickstockbot.com>

# Bot executable download URL (stubbed until Section 16 provides a signed URL)
BOT_DOWNLOAD_URL=https://download.quickstockbot.com/bot/latest

# SQLite path for the license database (defaults to <cwd>/licenses.db)
LICENSE_DB_PATH=

# Secret key required in X-Admin-Key header to call the revoke endpoint
ADMIN_API_KEY=
